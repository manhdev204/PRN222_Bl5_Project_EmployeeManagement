@model PRN222_BL5_Project_EmployeeManagement.Models.Department
@{
    ViewData["Title"] = "Create Department";
}
<h2>Create Department</h2>
<form method="post" asp-action="Create" asp-controller="AdminDepartments">
    <div class="mb-3">
        <label class="form-label">Department Name</label>
        <input class="form-control" asp-for="DepartmentName" />
    </div>
    <div class="mb-3 form-check">
        <input class="form-check-input" type="checkbox" name="DeleteFlag" value="true" @(Model?.DeleteFlag == true ? "checked" : "") />
        <label class="form-check-label">Delete Flag</label>
    </div>
    <div class="text-danger">@ViewBag.Error</div>
    <button class="btn btn-primary" type="submit">Create</button>
    <a class="btn btn-secondary" asp-action="Index" asp-controller="AdminDepartments">Back</a>
</form>


